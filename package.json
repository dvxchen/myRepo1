{
  "name": "gitaction",
  "version": "1.0.0",
  "scripts": {
    
    "test": "cypress run --reporter json --reporter-options output=./cypress/results/cypress-results.json"
  },
  "devDependencies": {
    "cypress": "^13.6.0",
    "nodemailer": "^6.9.8"
    
  }
}
